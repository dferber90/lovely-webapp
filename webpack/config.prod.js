const clientConfig = require('./config.client.prod');
const serverConfig = require('./config.server.prod');
const apiConfig = require('./config.api.prod');

module.exports = [clientConfig, serverConfig, apiConfig];
